// During quick development we use the Tailwind CDN (in index.html).
// Keep this file empty to avoid Vite trying to require `tailwindcss`
// when the npm package is not installed. When you install the
// `tailwindcss` and `autoprefixer` packages, restore the plugin
// configuration here.

module.exports = {};
